<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none text-white"
            type="text"
            placeholder="Title goes here.."
            name="title"
          />
          <textarea
            class="block px-5 py-2 bg-zinc-800 resize-none mt-2 w-full rounded-md outline-none text-white"
            placeholder="Write Your Task Details Here"
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md hover:bg-blue-500 active:bg-blue-600 cursor-pointer"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
          <a class="text-blue-500 inline-block mt-3" href="/file/<%= val%>"
            >Read More..</a
          >
        </div>
        <%})%> <%} else{ %>
        <h3 class="text-zinc-600">No Task yet</h3>
        <% }%>
      </div>
    </div>
    <!-- <h1>hello wanna calculate 10 + 10 is <%= 10+10 %></h1>-->
  </body>
</html>
